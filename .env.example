# ========================================
# Git Nest — 环境变量配置
# ========================================
# 复制此文件为 .env 并修改值：
#   cp .env.example .env
# ========================================

# --- 用户映射 ---
# NAS 上 git 用户的 UID/GID，确保容器以正确权限访问数据目录
PUID=1000
PGID=1000

# --- 安全 ---
# git-runner 与 nuxt-app 之间的共享密钥（必须修改！）
# 生成建议：openssl rand -hex 32
GIT_RUNNER_SECRET=change-me-to-a-random-string

# Web 界面登录密码（留空则不启用密码保护）
WEB_PASSWORD=

# --- 数据目录（宿主机路径）---
# Bare 仓库存储目录
GIT_DATA_DIR=/data/git
# 工作区目录（clone 后的目录）
GIT_WORKSPACE_DIR=/data/workspace

# --- 端口 ---
# Web 界面对外暴露端口
WEB_PORT=3000

# --- NAS 主机 ---
# NAS 的 IP 地址或域名（用于 SSH Clone URL 显示）
# 示例: 192.168.1.100 或 nas.your-domain.com
NUXT_PUBLIC_NAS_HOST=

# --- 备份 ---
# 备份存储目录
BACKUP_DIR=/data/backups
# 备份保留天数（超过此天数的旧备份自动清理）
BACKUP_RETENTION_DAYS=7
# 定时备份执行小时（0-23，默认凌晨3点）
BACKUP_SCHEDULE_HOUR=3

# --- code-server（在线编辑器）---
# code-server 对外暴露端口
CODE_SERVER_PORT=8443
# code-server 完整 URL（前端“Open in Editor”按钮跳转地址）
# 示例: http://nas-ip:8443 或 https://code.your-domain.com
CODE_SERVER_URL=
# code-server 登录密码（留空则无密码保护）
CODE_SERVER_PASSWORD=
# code-server sudo 密码（可选）
# CODE_SERVER_SUDO_PASSWORD=

# --- 可选配置 ---
# git 命令执行超时（秒），默认 30
# COMMAND_TIMEOUT=30
# 时区
# TZ=Asia/Shanghai
